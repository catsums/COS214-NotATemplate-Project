To run the detailed battle simulator code simple type the following commands in the System0 folder:
make
make run

To run the code simple version of the simulator, type the following commands in the System folder:
make
make run

To run the demo focusing on the actions, type the following commands in the System2 folder:
make
make run

When you are done you can run make clean to clear all .o files on any of the folders