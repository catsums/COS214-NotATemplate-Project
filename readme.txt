To run the code simple type the following commands in the System folder:
make
make run

To run the detailed battle simulator code simple type the following commands in the System2 folder:
make
make run

When you are done you can run make clean to clear all .o files